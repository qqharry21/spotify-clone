/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
  images: {
    domains: [
      'localhost:3000',
      'papa-spotify-clone',
      'platform-lookaside.fbsbx.com',
      'i.scdn.co',
      'mosaic.scdn.co',
    ],
  },
};

module.exports = nextConfig;
